/* eslint-env node */
'use strict';

module.exports = function(environment /*, appConfig */) {
  const ENV = {
    modulePrefix: 'ember-blog-engine',
    environment: environment
  }

  return ENV;
};
